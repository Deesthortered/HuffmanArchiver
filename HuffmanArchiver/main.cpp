#include "Array.h"
#include <iostream>
using namespace std;

int main()
{
	spaceArray::Array<int> a = spaceArray::Array<int>();
	/*
	a.Push_back(1);
	a.Push_back(2);
	a.Push_back(3);
	a.Push_back(4);
	a.Push_back(5);
	a.Push_back(6);
	a.Push_back(7);

	for (size_t i = 0; i < a.Size(); i++) cout << a[i] << endl;
*/	return 0;
}